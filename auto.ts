const date = new Date();
const config: any = {
  title: `Dev Blog ${date.getFullYear()}-${
    date.getMonth() + 1
  }-${date.getDate()}`,
  description: "a docs for daxiang dev 66",
};

export default config;
